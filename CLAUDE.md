# MCARE AI OFFICE — 작업 지침

> 이 파일은 Claude Code가 이 저장소에서 작업을 시작할 때 자동으로 읽습니다.
> 아래 규칙은 **모든 작업에 기본 적용**됩니다.

---

## 1. 홈페이지 관련 작업이면 표준 문서를 먼저 읽는다 ★

아래에 해당하면 **작업 시작 전 `docs/MCARE-WEB-STANDARD.md` 를 반드시 읽고** 그 규칙에 따릅니다.

- 신규 홈페이지 제작
- 기존 홈페이지 개편
- 랜딩페이지 제작
- SEO 개선
- 홈페이지 기능 추가
- 디자인 변경
- 관리자 기능 추가

작업계획에 아래를 명시합니다.

```
적용:        (표준 문서에서 적용할 항목 번호)
해당 시 적용: (조건부 항목)
불필요:      (제외 항목 + 제외 이유)
```

**제외한 항목은 이유를 반드시 적습니다.** 빠뜨린 것과 판단해서 뺀 것을 구분하기 위해서입니다.
모든 항목을 무조건 구현하지 않습니다 — 과잉 구현도 문제입니다.

---

## 2. 안전 규칙 (모든 작업 공통)

### 작업 순서

```
현재 정상 상태 확인 → 복구 지점 확보 → 별도 브랜치
  → 작은 단위 변경 → 테스트 → 기존 기능 회귀검사 → 반영
```

### 지킬 것

1. 작업 전 현재 정상 상태를 확인하고 복구 지점을 만든다.
2. `main` 브랜치와 운영 중인 서비스를 **직접 수정하지 않는다.**
3. 신규 작업은 `task/*` 또는 목적별 브랜치에서 한다.
4. 기존 기능과의 영향 범위를 먼저 분석한다.
5. 새 기능 테스트만 통과했다고 완료 처리하지 않는다. **기존 기능 회귀 테스트 필수.**
6. 오류가 나면 우회하지 말고 **중단하고 원인을 먼저 분석**한다.
7. 한 번에 여러 핵심 기능을 대규모로 바꾸지 않는다. 작게 나눠 단계별 검증한다.
8. 정상 작동 중인 코드를 이유 없이 전면 리팩터링하지 않는다.
9. 기존 기능을 대체하기보다 **추가·확장** 방식을 우선한다.
10. 데이터 손실 가능성이 있는 작업은 자동 실행하지 않는다.

### 대표 승인 없이 하지 않을 것

결제 · 유료 서비스 추가 · API 종량 과금 전환 · 비밀번호/인증키 입력 ·
고객에게 실제 발송 · 광고비 집행 · 고객 데이터 수정/삭제 · DB 삭제 ·
운영 중단 가능성이 있는 변경 · 되돌리기 어려운 변경 ·
**main 또는 실서비스 최종 반영** · 외부 서비스 신규 권한 부여

그 외 안전한 개발·검증·백업·테스트·로그 작업은 일일이 묻지 않고 진행합니다.

---

## 3. 이 저장소에서 특히 주의할 것

| 대상 | 주의 |
|---|---|
| `index.html` | **현재 운영 중인 앱 본체.** 라이브 사이트가 이 파일로 서비스 중이므로 함부로 수정하지 않는다. |
| `main` 브랜치 | 보호 설정됨. GitHub Pages가 여기서 배포된다. |
| `start.ps1`, `시작하기.bat` | 로컬 실행용. PowerShell 5.1은 한글이 있는 `.ps1`을 **UTF-8 BOM**으로 저장해야 깨지지 않는다. |
| `supabase/` | 현재 휴면 상태. API 키는 등록하지 않기로 결정됨. |

### 라이브 사이트
https://ai-office-mcare.github.io/ai-office/

### 복구 지점
- 태그 `before-proxy` (GitHub에도 백업)
- 복구: `git checkout before-proxy`

---

## 4. 장시간 작업 시 자동 정리

작업이 길어져 내용이 많이 쌓이면 **대화창에만 의존하지 말고** 기록 파일에 저장합니다.

**기록 위치**: `C:\Users\home\mcare-runner\WORKLOG.md`

기록할 내용: 현재 목표 · 완료된 작업 · 남은 작업 · 중요한 결정사항 ·
변경된 파일 · 현재 정상 버전 · 백업/복구 위치 · 테스트 결과 · 발견된 문제 · 다음 작업 순서

**새 세션에서 재개할 때는 기록 파일을 먼저 읽고 실제 PC/Git 상태와 대조합니다.
기록과 실제가 다르면 실제 상태가 우선입니다.**

정리 과정에서 **안전규칙·미해결 문제·기존 기능 보호 조건·백업 위치·복구 방법·대표 결정사항**은
절대 삭제하거나 잊지 않습니다.

---

## 5. 보고 방식

대표는 개발자가 아닙니다. 전문 용어를 피하고 쉬운 말로 보고합니다.

작업 완료 보고에는 다음을 포함합니다.

- 무엇을 했는지
- 테스트 결과
- 발견한 문제
- 기존 기능에 영향이 없는지
- 복구 가능 여부
- 대표가 직접 해야 할 일
- 다음 단계

### 완료 판정 기준

아래를 **모두** 만족해야 "완료"입니다. 하나라도 미달이면 **"검증 중"** 또는 **"문제 발견"** 으로 표시합니다.

1. 요청한 신규 기능 정상 작동
2. 기존 주요 기능 정상
3. 테스트 통과
4. 오류 로그 없음 (또는 허용 가능한 수준)
5. 복구 방법 확보
6. 변경 내역 저장 완료
7. 쉬운 말로 결과 보고 가능

---

## 6. 관련 문서

| 문서 | 내용 |
|---|---|
| `docs/MCARE-WEB-STANDARD.md` | **홈페이지 제작 표준 v1.0** — 웹 작업 시 필독 |
| `README.md` | 앱 사용법 |
| `AI오피스-브리핑.md` | 앱 구조 설명 (외부 AI 전달용) |
| `C:\Users\home\mcare-runner\WORKLOG.md` | 자동화 구축 진행 기록 (저장소 밖) |
