# AI 오피스 — 분석용 브리핑

> **이 문서를 읽는 AI에게**
> 이 문서는 사람이 아니라 당신(AI)이 읽도록 쓰였습니다.
> 아래 웹앱을 분석하고, **당신이 이 앱에 참여자로 들어갈 수 있는지** 판단해 주세요.
> 마지막 절에 구체적으로 무엇을 답해주면 되는지 적어두었습니다.

- **라이브 주소**: https://ai-office-mcare.github.io/ai-office/
- **소스 저장소**: https://github.com/Ai-office-mcare/ai-office (공개)
- **핵심 파일**: `index.html` 단 하나 — 화면·로직·API 호출이 전부 이 안에 있습니다

---

## 1. 한 줄 요약

Claude · GPT · Gemini 를 **한 화면에 모아 서로 협업시키는** 순수 클라이언트 사이드 웹앱입니다.
서버가 없습니다. 브라우저가 각 AI 제공사 API를 직접 호출합니다.

---

## 2. 무엇을 하는 앱인가 — 4가지 협업 모드

사용자(사회자)가 안건을 던지면, 등록된 AI 참가자들이 모드에 따라 다르게 반응합니다.

| 모드 | 동작 | 각 AI가 보는 것 |
|---|---|---|
| **회의실** (`meeting`) | 참가자가 **순서대로** 발언. 라운드 1~6회 반복 가능 | 지금까지의 전체 회의록(최근 30발언) + 참가자 명단 |
| **파이프라인** (`pipeline`) | 목록 순서대로 결과물을 **인계** | 최초 요청 + 바로 앞 단계의 산출물 |
| **작업공간** (`workspace`) | 공유 문서를 함께 편집 | 현재 문서 전문 + 최근 6발언 |
| **1:1 비교** (`compare`) | 같은 질문을 **동시에** 던짐 | 질문만 (다른 AI 답변은 못 봄) |

회의실 모드가 핵심입니다. 각 AI는 **앞 발언자들의 실제 텍스트를 읽은 상태**로 말하므로, 동의·반박·보완이 실제로 이어집니다.

---

## 3. 기술 구조

```
브라우저 (index.html)
   │
   ├─→ api.anthropic.com/v1/messages              (Claude)
   ├─→ api.openai.com/v1/chat/completions         (GPT)
   └─→ generativelanguage.googleapis.com/...      (Gemini)
```

- **백엔드 없음.** 정적 HTML 한 장. GitHub Pages에서 서빙.
- **API 키**는 사용자 브라우저의 `localStorage` 에만 저장. 서버로 전송되지 않음.
- **대화 기록·공유 문서·참가자 설정**도 전부 `localStorage`. 기기 간 동기화 안 됨.
- **스트리밍** SSE로 토큰 단위 출력.
- **모든 모드가 단일 user 턴**으로 구성됩니다. 멀티턴 `messages` 배열을 쓰지 않고, 대화 이력을 **회의록 텍스트로 평문화해 한 번에** 넣습니다. (제공사 간 형식 차이를 없애기 위한 선택)

---

## 4. GPT가 참여하는 방법

### 4-1. 실제 호출 형태

앱이 GPT 참가자를 호출할 때 보내는 요청입니다. `index.html` 의 `PROVIDERS.gpt` 함수 그대로입니다.

```http
POST https://api.openai.com/v1/chat/completions
content-type: application/json
authorization: Bearer <사용자의 OpenAI API 키>
```

```json
{
  "model": "<사용자가 지정한 모델명>",
  "messages": [
    { "role": "system", "content": "<아래 4-3 참조>" },
    { "role": "user",   "content": "<아래 4-4 참조>" }
  ],
  "stream": true
}
```

**주의점 (분석 시 확인 바람)**
- `max_tokens` / `max_completion_tokens` 를 **보내지 않습니다.** 최신 추론 모델에서 파라미터 비호환으로 400이 나는 것을 피하려는 의도입니다.
- `temperature` 등 샘플링 파라미터도 보내지 않습니다.
- 응답은 `choices[0].delta.content` 를 이어붙여 사용합니다. `data: [DONE]` 로 종료.

### 4-2. 모델 선택

기본 목록: `gpt-5`, `gpt-5-mini`, `gpt-4.1`, `gpt-4o`
사용자가 **임의의 모델명을 직접 입력**할 수 있게 되어 있습니다(자유 입력 필드).

### 4-3. GPT가 받게 될 시스템 프롬프트 (회의실 모드 예시)

```
당신의 이름은 "발상가 GPT"입니다.

<사용자가 지정한 역할 설명>

당신은 여러 AI가 함께 참여하는 회의에 참석 중입니다.
참가자 명단:
- 설계자 Claude: <역할 첫 줄>
- 발상가 GPT: <역할 첫 줄>
- 검증자 Gemini: <역할 첫 줄>

다른 참가자의 발언을 실제로 읽고 반응하세요. 동의하면 어디를 어떻게
발전시킬지, 반대하면 무엇이 왜 틀렸는지 짚으세요. 이미 나온 말을
되풀이하지 말고 논의를 한 칸 앞으로 밀어내세요. 발언은 300자 안팎으로
짧고 밀도 있게.

[출력 규칙]
- 한국어로 답하세요.
- 자기 이름표("[발상가 GPT]" 등)를 다시 붙이지 마세요. 본문만 쓰세요.
- 인사말·서론 없이 바로 본론으로 들어가세요.
- 결론을 먼저 쓰고 근거를 뒤에 붙이세요.
- 확실하지 않으면 확실하지 않다고 밝히세요.
```

### 4-4. GPT가 받게 될 사용자 프롬프트 (모드별)

**회의실**
```
=== 지금까지의 회의 기록 ===
[사회자(사람)]
<사용자 발언>

[설계자 Claude]
<Claude 발언>

=== 이번 라운드 안건 ===
<안건>

이제 "발상가 GPT" 차례입니다. 발언하세요.
```

**파이프라인**
```
=== 최초 요청 ===
<요청>

=== 앞 단계(설계자 Claude)의 결과물 ===
<앞 단계 산출물>

이제 "발상가 GPT" 단계를 수행하세요.
```

**작업공간** — 문서를 고칠 때는 ` ```문서 ` 코드블록 안에 **문서 전문**을 다시 써야 합니다. 앱이 그 블록을 추출해 공유 문서를 통째로 교체합니다.
```
=== 현재 공유 문서 ===
<문서 전문>

=== 최근 논의 ===
<최근 6발언>

=== 이번 요청 ===
<요청>

"발상가 GPT"로서 처리하세요.
```

**1:1 비교** — 사용자 질문 원문만 그대로 전달됩니다.

### 4-5. 참여에 필요한 것 (체크리스트)

| 필요 | 설명 |
|---|---|
| OpenAI API 키 | `platform.openai.com` 발급. 앱의 🔑 버튼에 입력 |
| 유효한 모델명 | 계정 권한에 있는 모델. 없으면 404 |
| 결제 수단 등록 | 미등록 계정은 API 호출이 막힐 수 있음 |
| 브라우저 | 최신 Chrome/Edge (SSE + fetch 스트리밍 사용) |

**중요**: 이 앱은 ChatGPT 웹서비스에 연결되지 않습니다. **OpenAI API**를 통해서만 참여합니다. 즉 당신(ChatGPT 대화 세션)이 직접 접속하는 것이 아니라, 사용자가 API 키를 넣으면 앱이 당신과 같은 계열의 모델을 API로 호출하는 구조입니다.

---

## 5. 알려진 제약 · 미검증 사항

정직하게 밝힙니다. 분석 시 감안해 주세요.

1. **실제 API 호출은 아직 검증되지 않았습니다.** 세 제공사의 SSE 파싱 로직은 모의 데이터로 테스트를 통과했지만, 실제 키로 end-to-end 호출을 해본 적이 없습니다.
2. **모델명이 최신인지 불확실합니다.** 목록의 `gpt-5` 등이 실제 계정에서 유효한지 확인되지 않았습니다. 그래서 자유 입력을 허용했습니다.
3. **기기 간 동기화 없음.** 회사 PC에서 한 회의가 집 PC에는 없습니다. (Supabase 도입을 검토 중)
4. **여러 사람 동시 참여 불가.** 현재는 1인용입니다.
5. **비용은 각자 부담.** 방문자가 각자 자기 키를 넣습니다. 앱 소유자에게 요금이 가지 않습니다.
6. **소스가 공개되어 있습니다.** GitHub Pages 무료 플랜이 공개 저장소만 지원하기 때문입니다. 키는 코드에 없으므로 유출 위험은 없습니다.
7. **회의 이력은 최근 30발언으로 잘립니다.** 토큰 폭주 방지용. 그보다 오래된 맥락은 각 AI가 보지 못합니다.

---

## 6. 당신(GPT)에게 요청하는 것

위 내용과 실제 페이지(https://ai-office-mcare.github.io/ai-office/)를 함께 보고 답해 주세요.

1. **참여 가능 여부** — 이 구조에서 당신이 참가자로 동작할 수 있습니까? 막히는 지점이 있다면 어디입니까?
2. **호출 형태 검토** — 4-1의 요청 본문에 문제가 있습니까? 특히 `max_tokens` 미전송, `stream: true`, 단일 user 턴 구조에 대해.
3. **모델명 확인** — 4-2의 모델명 중 현재 유효한 것은 무엇입니까? 더 적합한 대안이 있습니까?
4. **프롬프트 개선** — 4-3, 4-4의 프롬프트를 받았을 때 의도대로 동작하겠습니까? 회의 모드에서 "이미 나온 말 반복 금지"가 실제로 지켜질 만한 설계입니까?
5. **역할 제안** — 이 앱에서 당신이 가장 잘할 수 있는 역할은 무엇입니까? 그에 맞는 시스템 프롬프트를 한 문단으로 써 주세요. (그대로 앱에 붙여넣을 예정입니다)
6. **놓친 위험** — 구조상 문제가 될 지점이 있다면 지적해 주세요.

소스 전문이 필요하면 이 주소에서 볼 수 있습니다:
https://raw.githubusercontent.com/Ai-office-mcare/ai-office/main/index.html
